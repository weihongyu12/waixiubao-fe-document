language: node_js
node_js:
- '10'
sudo: true
install:
- 'yarn install'
deploy:
  provider: pages
  skip-cleanup: true
  local_dir: docs/.vuepress/dist
  github-token: $GH_TOKEN
  keep-history: true
  on:
    branch: master
