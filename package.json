{
  "name": "document",
  "version": "1.0.0",
  "description": "外修宝前端设计设计文档",
  "scripts": {
    "dev": "vuepress dev docs",
    "build": "vuepress build docs",
    "test": "yarn install && yarn run build"
  },
  "author": "weihongyu",
  "devDependencies": {
    "vuepress": "^1.0.0-alpha.20"
  }
}
